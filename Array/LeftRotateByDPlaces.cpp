/*
    Problem Statement:
        Rotate the array to the left by D places.
*/

#include <bits/stdc++.h>
using namespace std;

// void leftRotateByOne()

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    int n = sizeof(arr)/sizeof(int);
    // leftRotate(arr, n, 2);
    for (auto x: arr) 
        cout << x << " ";
}
